# Sample environment configuration for Kevlar

# Google Cloud project ID
PROJECT_ID=your-gcp-project
# BigQuery dataset ID where KEV and inventory tables live
DATASET_ID=kevlar_dataset
# Name of the KEV table (without dataset prefix)
TABLE_ID=kevs

# Jira integration
JIRA_URL=https://yourcompany.atlassian.net
JIRA_USER=you@example.com
JIRA_TOKEN=your-jira-api-token
JIRA_PROJECT_KEY=SEC

# Health mode toggle for scheduled runs
HEALTH_MODE=false
# Time to wait after loading data into BigQuery before correlation (seconds)
BQ_WAIT_TIME=10
# Attach CSV of matches to Jira health ticket (true/false)
ATTACH_CSV=false
